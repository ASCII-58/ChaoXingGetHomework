# 前端重写需求

## 1. 页面结构

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录 | `/login` | 密码登录 + Cookie 登录 |
| 作业列表 | `/homework` | 主页面，筛选 + 统计 + 课程分组卡片 |
| 设置 | `/settings` | Cookie管理、手机号、通知开关、关于 |

## 2. 布局

左（侧边栏固定 220px）右（主内容区）布局：

```
┌─────────┬─────────────────────────────────────┐
│ sidebar │ main content                         │
│ 220px   │                                     │
│ ─────── │  [统计行] [筛选栏]                    │
│ 📋 作业  │  ┌──────┬──────┬──────┬──────┐      │
│ ⚙️ 设置  │  │全部 N │待提交N│已完成N│已截止N│      │
│         │  └──────┴──────┴──────┴──────┘      │
│ ● 已连接 │  [搜索框] [状态筛选] [课程筛选]       │
│         │  ─────────────────────────────────   │
│         │  📁 课程名 (5项作业)                  │
│         │  ┌───────────────┐                   │
│         │  │ 📝 作业标题    │ 待提交  剩余3天   │
│         │  │ 课程名         │                  │
│         │  └───────────────┘                   │
└─────────┴─────────────────────────────────────┘
```

侧边栏：

- 顶部 Logo + 应用名
- 导航链接：作业、设置
- 底部：会话状态指示（圆点 + 文字）

## 3. 登录页

- 密码登录表单（手机号 + 密码）
- Cookie 登录（textarea 粘贴 + 设置/清除按钮）
- 两种方式成功后均自动跳转至 `/homework`
- 登录成功后自动触发作业数据加载

## 4. 作业列表页

### 4.1 数据加载

- 进入页面时自动调用 `fetchHomeworkList()` + `fetchCourseList()`
- 缓存数据在 `appState.items` 中
- 提供"刷新"按钮手动重新加载
- 加载过程中显示加载状态

### 4.2 统计行

- 全部（总数）
- 待提交（Pending 数量）
- 已完成（Completed 数量）
- 已截止（Overdue 数量）
- 每个统计项可点击作为快捷筛选

### 4.3 筛选栏

- 搜索框：按作业标题关键词实时过滤
- 状态筛选 pills：全部 / 待提交 / 已完成 / 已截止
- 课程下拉菜单：全部课程 / 具体课程名称

筛选逻辑（前端对缓存数据执行）：

```
status === 'all' → 不过滤
status === 'pending' → status_code === 'Pending'
status === 'completed' → status_code === 'Completed'
status === 'overdue' → status_code === 'Overdue'
search → item.title.includes(search text)
course === 'all' → 不过滤
course → item.course_id === Number(course)
```

### 4.4 作业卡片

- 按课程分组，组标题显示课程名和作业数量
- 每张卡片可点击，打开对应课程页面
- 卡片内容：
  - 标题（粗体）
  - 未读指示（红点）
  - 状态标签（彩色 badge）
  - 截止时间
  - 课程名

## 5. 设置页

### 5.1 会话状态

- 当前会话状态（有效/过期/未连接）
- "检查会话"按钮

### 5.2 Cookie 管理

- 当前 Cookie 显示（textarea，只读?）
- "设置 Cookie" 按钮
- "清除 Cookie" 按钮

### 5.3 手机号管理

- 手机号输入框
- "保存"按钮

### 5.4 通知开关

- 开关 toggle：启用/关闭桌面通知
- 默认启用
- 状态持久化（localStorage）

### 5.5 关于

- 应用名称
- 版本号
- GitHub 链接

## 6. 交互要求

- 登录成功 → `router.navigate('/homework')` + 自动加载数据
- 筛选操作实时生效（无网络请求）
- 点击作业卡片 → 调用 `shell.open(url)` 在默认浏览器打开课程页
- Toast 通知反馈所有操作结果
- 侧边栏当前页面高亮

## 7. 涉及文件

| 文件 | 改动 |
|------|------|
| `index.html` | 替换导航为侧边栏布局 |
| `src/style.css` | 重写 — 侧边栏、筛选栏、卡片、开关、设置页样式 |
| `src/main.js` | 重写 — 新增筛选逻辑、设置页、自动跳转、通知开关 |

## 8. 不做改动

- Rust 后端代码（所有 Tauri commands）
- `src/tauri-login.js`
- `src/router.js`
- `src/homework.js`
- `tests/`
