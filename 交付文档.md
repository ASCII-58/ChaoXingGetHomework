# 学习通助手 — 交付文档

## 1. 项目概述

桌面 GUI 应用，通过学习通的逆向 API 获取所有作业，并在作业截止前通过原生桌面通知提醒用户。

- **前端**: Vanilla JS (ESM) + Vite 构建
- **桌面壳**: Tauri (Rust 后端 + WebView 前端)
- **包管理**: npm
- **测试**: Vitest
- **代码检查**: ESLint

## 2. 项目结构

```
xxt/
├── index.html              # 入口 HTML — 侧边栏布局
├── package.json             # 依赖与脚本
├── vite.config.js           # Vite + Vitest 配置
├── eslint.config.js         # ESLint flat 配置
├── AGENTS.md                # AI 代理行为说明
├── API.md                   # Chaoxing 逆向 API 文档
├── REQUIREMENTS.md          # 前端重写需求文档
├── 交付文档.md              # 本文件
│
├── src/
│   ├── main.js              # 主应用 — UI 渲染、事件、状态管理
│   ├── router.js            # Hash-based 路由（不可修改）
│   ├── homework.js          # 作业 HTML 解析器（不可修改）
│   ├── login.js             # 登录逻辑（Node.js 侧 AES 加密）
│   ├── tauri-login.js       # Tauri invoke 代理层（不可修改）
│   └── style.css            # 全部样式
│
├── src-tauri/
│   ├── Cargo.toml           # Rust 依赖
│   ├── tauri.conf.json      # Tauri 窗口/安全配置
│   └── src/main.rs          # Rust 后端 — 所有 Tauri commands（不可修改）
│
├── tests/
│   ├── login.test.js        # 登录加密/错误处理测试（18 项）
│   └── homework.test.js     # HTML 解析器测试（14 项）
│
└── dist/                    # Vite 构建产物
```

## 3. 页面与路由

| 路由 | 页面 | 说明 |
|------|------|------|
| `#/login` | 登录页 | 密码登录 + Cookie 登录 |
| `#/homework` | 作业列表 | 统计行 + 筛选栏 + 课程分组卡片 |
| `#/settings` | 设置页 | 会话、Cookie、手机号、通知、忽略管理 |

## 4. 关键文件详解

### 4.1 `index.html`

侧边栏 (220px 固定) + 主内容区布局。侧边栏包含：
- 顶部：Logo + 应用名
- 中间：导航链接（📋 作业、⚙️ 设置）
- 底部：会话状态指示（圆点 + 文字）

右侧是 `<main id="app-content">`，各页面渲染到此容器中。Toast 通知悬浮在右下角。

### 4.2 `src/main.js`

**核心入口**。职责：

#### 状态管理 (`appState`)

```js
{
  items: [],           // 作业列表（来自 fetchHomeworkList）
  courses: [],         // 课程列表（来自 fetchCourseList）
  hasCookie: bool,
  cookie: string|null,
  phone: string,
  notifications: bool,  // 桌面通知开关
  filterStatus: "all" | "pending" | "completed" | "overdue",
  filterSearch: string,
  filterCourse: "all" | course_id,
  statsFilter: "all" | ...,
  isRefreshing: bool
}
```

#### localStorage 持久化键

| 键 | 说明 |
|---|------|
| `xxt_hw_cache` | 作业/课程缓存 `{ items, courses, ts }` |
| `xxt_notifications` | 通知开关 |
| `xxt_ignore_courses` | 忽略的课程 ID 数组 |
| `xxt_ignore_homework` | 忽略的作业 ID 数组 |
| `xxt_notified_ids` | 已发通知的作业 ID 数组（防重复） |

#### 页面渲染

- **`renderLogin()`** – 两个卡片：密码登录（手机号+密码表单）、Cookie 登录（textarea + 设置/清除）。登录成功自动导航到 `/homework`。
- **`renderHomework()`** – 顶部标题+刷新按钮，统计行（四色 pill），筛选栏（搜索框+状态 pills+课程下拉），作业内容区。自动调用 `loadHomeworkData()`。
- **`renderSettings()`** – 会话状态+检查按钮，Cookie 管理（显示/设置/清除），手机号保存，通知开关 toggle，已忽略列表，关于信息。

#### 缓存机制 (`loadHomeworkData`)

1. 尝试从 `localStorage` 加载缓存（`xxt_hw_cache`）
2. 有缓存 → 立即渲染，显示"正在刷新..."指示器，后台静默拉取新数据
3. 无缓存 → 显示加载 spinner
4. 后台请求完成 → 替换 `appState`，更新视图，隐藏指示器，发通知，保存新缓存
5. 请求失败且无缓存 → 显示错误消息

#### 筛选逻辑 (`getFilteredItems`)

按顺序应用：
1. 排除已忽略的课程
2. 排除已忽略的作业
3. 状态筛选（status_code 匹配）
4. 搜索关键词（标题 includes）
5. 课程下拉（course_id 匹配）

#### 忽略机制

- 作业卡片右上角 **✕** 按钮 → 忽略单个作业
- 课程标题行 **忽略课程** 按钮 → 忽略整门课程
- 设置在"已忽略"区域可取消忽略课程、清除所有忽略作业
- 忽略后统计行和筛选下拉菜单实时更新

#### 桌面通知 (`sendNotifications`)

1. 检查 `appState.notifications` 开关 + `Notification` API 可用性
2. 请求权限（如未授权）
3. 遍历作业，筛选 `status_code === "Pending"` 且截止时间 < 24 小时的
4. 匹配规则：`剩余0天`、`剩余1天`、`剩余*小时`
5. 用 `xxt_notified_ids` 防重复通知
6. 通过 `new Notification("作业即将截止", { body })` 发送

#### 事件处理

全部通过事件委托在 `#app-content` 上处理（click / submit / input / change），包括：
- 作业卡片点击 → `shell.open()`（Tauri）或 `window.open()`（浏览器 fallback）
- 统计行/状态 pills 点击 → 切换筛选
- 忽略按钮 → 持久化 + 刷新视图
- 未忽略按钮 → 从 localStorage 移除 + 重渲染设置页

### 4.3 `src/style.css`

**设计系统**:
- CSS 变量配色，支持 `prefers-color-scheme: dark` 自动暗色模式
- 统一间距、圆角、字体、过渡动画

**主要样式模块**:
- `.app-layout` / `.sidebar` / `.main` — 固定侧边栏布局
- `.stat-pill` — 可点击统计 pill（active 状态高亮）
- `.filter-bar` / `.filter-search` / `.filter-status-btn` / `.filter-course` — 筛选栏
- `.homework-section` / `.homework-group-header` / `.homework-card` — 作业分组卡片
- `.homework-card-actions` / `.btn-ignore-hw` / `.btn-ignore-group` — 忽略按钮
- `.refresh-indicator` — 动画脉冲"正在刷新..."
- `.toggle` / `.toggle-slider` — CSS 开关
- `.settings-section` / `.ignored-item` — 设置页
- `.toast` — 固定右下角通知

### 4.4 `src/router.js`

简易 hash-based 路由（**不可修改**）:
- 监听 `hashchange` 事件
- `route(path, handler)` 注册路由
- `navigate(path)` 跳转（`location.hash = path`）
- `start()` 初始化（默认跳转到 `/login`）

### 4.5 `src/tauri-login.js`

Tauri invoke 代理层（**不可修改**）:
- 检测是否在 Tauri 环境（`window.__TAURI_IPC__`）
- 非 Tauri 环境直接 reject（浏览器中无法调用 Rust 命令）
- 导出函数：`loginWithPassword`、`setCookie`、`getCookie`、`clearCookie`、`checkSession`、`savePhone`、`loadConfig`、`fetchCourseList`、`fetchHomeworkList`

### 4.6 `src/homework.js`

作业 HTML 解析器（**不可修改**）:
- `parseHomeworkHtml(html)` 解析 `/work/stu-work` 返回的 HTML
- 提取字段：`title`, `status_label`, `status_code`, `course_name`, `deadline`, `task_url`, `unread`, `course_id`, `class_id`
- 状态映射:
  - `已完成` / `已批阅` / `待批阅` → `Completed`
  - `未提交` / `未交` / `已提交` → `Pending`
  - `已截止` → `Overdue`

### 4.7 `src/login.js`

Node.js 端登录函数（用于测试，AES 加密实现）:
- AES-128-CBC 加密（Key/IV: `u2oh6Vu^HWe4_AES`）
- POST `https://passport2.chaoxing.com/fanyalogin`
- 返回 `{ status, name, cookies }`
- 检测 captcha/block

## 5. Rust 后端 (`src-tauri/src/main.rs`)

**不可修改**。提供 Tauri commands:

| Command | 说明 |
|---------|------|
| `login_with_password` | AES 加密→POST 登录→提取 cookie |
| `set_cookie` / `get_cookie` / `clear_cookie` | Cookie 持久化至 `config.json` |
| `check_session` | 请求 `/work/stu-work` 验证会话有效性 |
| `save_phone` / `load_config_state` | 手机号持久化 |
| `fetch_course_list` | 请求 `/mycourse/backclazzdata` → 解析课程列表 |
| `fetch_homework_list` | 请求 `/work/stu-work` → 解析作业列表 |
| `resolve_task_url` | 跟踪作业链接跳转 → 提取最终作业 URL |

持久化文件路径：`~/.local/share/xxt/config.json`（Linux）或 `%APPDATA%/xxt/config.json`（Windows）。

## 6. 构建与运行

```bash
npm install          # 安装依赖
npm run dev          # Vite 开发服务器（浏览器预览）
npm run build        # Vite 生产构建
npm test             # 运行所有测试
npm run lint         # ESLint 检查
npm run tauri        # Tauri 桌面应用开发模式
npm run tauri:build  # 打包桌面应用
```

## 7. 测试覆盖

**18 项测试，全部通过** (`npm test`):

| 文件 | 测试数 | 覆盖功能 |
|------|--------|----------|
| `tests/login.test.js` | 4 | AES 加密验证、cookie 提取、错误密码、captcha 检测 |
| `tests/homework.test.js` | 14 | HTML 解析、标题/状态/课程/截止时间/URL/未读标记提取、course_id/class_id、边界条件（空 HTML、缺失状态） |

**代码检查**: ESLint flat config，0 errors。

## 8. API 參考

完整逆向 API 文档见 `API.md`，涵盖：
- 登录流程（AES 加密 + POST + captcha 检测）
- 课程列表 API (`/mycourse/backclazzdata`)
- 作业列表 API (`/work/stu-work`, `/mooc2/work/list`)
- Token 系统（`enc`, `openc`, `workEnc` 等）

## 9. 遗留问题与注意事项

1. **Rust 端状态映射**：`main.rs:362-369` 的 `status_code()` 函数中"待批阅"仍映射为 `"Pending"`。当前前端 `homework.js` 已改为 `"Completed"`，但 Rust 侧未同步。实际上前端 `homework.js` 的映射未被 Rust 使用 — Rust 有自己的解析逻辑，两者独立存在。如需一致需同步修改 Rust 侧的 `status_code` 函数。
2. **通知权限**：需用户在系统中允许应用发送通知。首次触发时浏览器/WebView 会弹出权限请求。
3. **通知去重**：基于 `makeHwId()`（`course_id_class_id_title`）在 localStorage 中记录已通知 ID，避免重复弹窗。
4. **缓存粒度**：当前缓存全量数据到 localStorage。如果作业数量极大（500+），可能接近 5MB 存储上限，可考虑只缓存关键字段或分页存储。
5. **忽略标识持久化**：忽略的课程/作业列表存储在 localStorage，清除浏览器数据会丢失。
